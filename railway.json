{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install --prefix backend && npm run build --prefix frontend"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyMaxRetries": 3
  }
}
